> Research input — non-authoritative and non-executable.
> This document must not be used as project truth and must not be executed.

--- name: graph-engineering description: >-   AI 短剧平台统筹作业系统的入口(R167/R168):四层真源指针+资源判据命令+发针模板+巡检序列。   触发:接手统筹/新会话冷启动/巡检/派单/发针/合流/验收前查判据;或用户提及 graph engineering、   工作系统、统筹机制。本 skill 是稳定入口层,真身在集成 worktree 的 docs——冲突一律以 docs 为准。 metadata:   version: "1.0.0"   created: "2026-08-03"   origin: DC R168「项目文档并不稳定,需要进系统机制,skill 也可以分为 user 和项目级」 ---  # graph engineering 入口(项目级 skill,工作区根=不随 git 分支漂)  ## 唤起时的自进化动作(R169,DC 2026-08-03)  每次唤起本 skill,先查 `outputs/graph-research-<今日>.md` 是否已存在: - 不存在 → 用 Agent 工具派一个 **model=sonnet 的后台调研 subagent**(run_in_background),任务:WebSearch 检索最近 1-3 个月 graph engineering / multi-agent orchestration / agent workflow 的研究与工程实践新进展(论文、框架 release、大厂实践帖),产出=对照我们四层体系「可借鉴/不适用/已领先」三栏简报,落 `outputs/graph-research-<日期>.md`;主线不等它,结果到了在巡检轮消化,有升级价值的走 R 号进 system-graph 修订。 - 已存在 → 跳过(当日一次,防重复烧钱)。 演化史锚(DC 观察):prompt→context→harness→loop→graph,三年五代——本体系必须假设自己会被下一代替掉,调研的目的就是先看见下一代。  ## 四层真源(真身全在集成 worktree,本文只是指针)  | 层 | 真身 | 管什么 | |---|---|---| | 规矩层 | ~/.claude/skills/dc-ground-rules(user 级 skill) | DC 协作硬规矩,跨项目 | | 结构层 | `.worktrees/v011-integration/docs/planning/system-graph.md`(v3) | 节点/边/资源/稳态机制 | | 操作层 | `.worktrees/v011-integration/docs/planning/fable-handover.md` | 命令、判据、恢复序列 | | 协作层 | `docs/delivery-workflow.md`(工作区根) | 三层派发结构 |  账本:`docs/planning/orchestration.md`(R 表+资源注册簿+迁移台账+DAG)。 以上路径基址=`/Users/user/Workspace/02_AI工具开发/`。  ## 接手序列(压缩版,完整版=handover §0)  1. 读 handover 全文 → orchestration 最后两节 → version-plan 在途档; 2. 状态只认命令不认记忆(handover §1 命令表); 3. 从 §2 七步循环的中断处继续。  ## 高频判据速查(真身 handover ⑤ 系,此处只列最常用)  - **发针前验退(⑤e,硬化模板)**:`cnt=$(ps aux | grep '/Users/user/.kimi-code/bin/kimi' | grep -v grep | grep -c resume); [ "$cnt" -gt 0 ] && 拒发`;sol 按可执行路径+启动时间数,bash 残壳的命令文本会假匹配。 - **E2E 全局互斥(⑤f)**:`ps aux | grep -E "[p]laywright.*(ux-baseline|zeroguide|smoke)"` >0 不派 E2E(047 单活跃会话,并发套件登录互踢=数据必脏)。 - **异步动作钉死引用(graph v3 边约束)**:合流锚 tag/commit 禁分支名;迁移前盘活连接;发针前验退——同一病根的三个面。 - **8000 三元组读回**:服务换代后 health/login/最新路由三项读回全绿才算完成(handover §1)。 - **资源注册簿**:orchestration「一之五」节——持有人一律现查不登记。  ## 门禁链(commit/push 前)  术语门禁+plan-freshness:`node --import tsx --test test/doc-terminology.test.ts test/plan-freshness.test.ts`,**判定必须落文件后 `grep -q "fail [1-9]"`,禁止管道 grep 接 commit(退出码会被吃)**。push=pre-push hook 全量,唯一门禁,永不双跑;成败只认 fetch 后水位。  ## 与 user 级 skill 的分界  跨项目通用(DC 协作规矩/思考本)=user 级;本项目工作系统(本文+四层真源)=项目级(工作区根,单份不漂)。项目细节永不写回 user 级 skill。
